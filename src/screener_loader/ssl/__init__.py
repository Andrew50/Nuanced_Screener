"""
Self-supervised learning utilities for market-shape sequence encoders.

This package is intentionally lightweight at import time; heavy deps (torch)
should be imported lazily inside modules/commands that require them.
"""

